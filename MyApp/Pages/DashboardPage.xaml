<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MyApp.Views"
    x:Class="MyApp.Pages.DashboardPage">
	<ContentPage.Content>
        <StackLayout Orientation="Vertical"
            VerticalOptions="FillAndExpand">
            <StackLayout Orientation="Horizontal">
                <Label Text="Welcome" />
                <Label Text="{Binding User.FullName}" />
            </StackLayout>
            <views:ListWidgetView ListResult="{Binding WidgetOptions[0].ListOptions.ListResults}"
                Title="{Binding WidgetOptions[0].ListOptions.Title}"
                IsLoading="{Binding WidgetOptions[0].ListOptions.IsLoading}"
                HandleItemSelected="{Binding WidgetOptions[0].ListOptions.ListNavigateCommand}"
                VerticalOptions="FillAndExpand">
            </views:ListWidgetView>
        </StackLayout>
	</ContentPage.Content>
</ContentPage>
